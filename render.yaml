services:
  - type: web
    name: trading-webhook
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --workers 1 --threads 2 --timeout 120 --log-level info"
    plan: free
